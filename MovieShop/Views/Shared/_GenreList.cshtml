@*
    For more information on enabling MVC for empty projects, visit https://go.microsoft.com/fwlink/?LinkID=397860
*@
@{
}
@* <div class="row row-cols-auto ms-1 me-1 mb-0">
    @foreach (Genre genre in ViewBag.Genre)
    {
        if ((int?)ViewData["id"] == genre.Id)
        {
            <a class="btn btn-primary mb-1 ms-1 me-1 active rounded-0" asp-action="MoviesByGenre" asp-controller="Movie"
               asp-route-id="@genre.Id" asp-route-pageNumber=1>@genre.Name</a>
        }
        else{
            <a class="btn btn-secondary mb-1 ms-1 me-1 rounded-0" asp-action="MoviesByGenre" asp-controller="Movie"
                asp-route-id="@genre.Id" asp-route-pageNumber=1>@genre.Name</a>
        }
    }

    
</div> *@

<div class="dropdown">
    <a class="btn btn-secondary dropdown-toggle" href="#" role="button" id="GenreMenuLink" data-bs-toggle="dropdown" aria-expanded="false">
        Genre
    </a>
    <ul class="dropdown-menu" aria-labelledby="GenreMenuLink">
        @foreach (Genre genre in ViewBag.Genre)
        {
            <li>
                <a class="dropdown-item" asp-action="MoviesByGenre" asp-controller="Movie"
                   asp-route-id="@genre.Id" asp-route-pageNumber=1>@genre.Name</a>
            </li>
        }
    </ul>
</div>